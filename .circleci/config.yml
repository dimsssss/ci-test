
version: 2
jobs: 
  build:
    docker: 
      image: ubuntu:18.04
    steps:
      - checkout # this pulls code down from GitHub
      - run: npm install
  test:
    steps:
      - run: npm run test
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test